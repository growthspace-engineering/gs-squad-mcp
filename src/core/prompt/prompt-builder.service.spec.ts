describe('PromptBuilderService', () => {
  test.todo('stateless prompt contains role body, task, and footer');
  test.todo('stateful new chat contains role body, initial task, and footer');
  test.todo('stateful existing chat contains task only and footer');
});

