describe('SquadConfigService', () => {
  test.todo('should provide default values when env vars are not set');
  test.todo('should override defaults with env vars when provided');
  test.todo('should throw error for invalid STATE_MODE');
  test.todo(
    'should throw error when stateful mode missing CREATE_CHAT_TEMPLATE_PATH'
  );
});

