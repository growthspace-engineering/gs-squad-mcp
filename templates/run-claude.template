claude -p --output-format=json<% if (typeof chatId !== undefined && chatId) { %> --session-id "<%= chatId %>"<% } %> "<%= prompt %>" \
  | jq -rc '.result.content[] | select(.type == "text") | .text'

